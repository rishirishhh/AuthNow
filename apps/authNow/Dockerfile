# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build @auth-now/authNow`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t auth-now-authnow`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system auth-now-authnow && \
          adduser --system -G auth-now-authnow auth-now-authnow

COPY apps/authNow/dist auth-now-authnow/
COPY apps/authNow/package.json auth-now-authnow/
RUN chown -R auth-now-authnow:auth-now-authnow .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix auth-now-authnow --omit=dev -f install

CMD [ "node", "auth-now-authnow" ]
